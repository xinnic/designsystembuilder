# react-native (pulled in transitively by Tamagui) wants @types/react 19 while
# this app is on 18. Matches the root .npmrc — npm reads .npmrc from the cwd
# only, so a build that cds into this directory needs its own copy.
legacy-peer-deps=true
